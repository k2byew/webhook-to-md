{
  "name": "webhook-to-md",
  "version": "1.0.0",
  "description": "A HTTP server which receives webhook from WordPress and writes the data to a MD file usable as a Jekyll post.",
  "main": "webhook-to-md.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "simple-git": "^1.11.0"
  }
}
